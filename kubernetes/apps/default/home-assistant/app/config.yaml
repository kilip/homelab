---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant-config
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: registry.k8s.io/git-sync/git-sync
      tag: v3.6.5
    env:
      GITSYNC_REPO: https://github.com/kilip/hass-config
      GITSYNC_BRANCH: main
      GITSYNC_ROOT: /hass-config
      GITSYNC_LINK: current
      GITSYNC_ONE_TIME: "false"
      GITSYNC_ADD_USER: "true"
      GITSYNC_USERNAME: "${GITSYNC_USERNAME}"
      GITSYNC_PASSWORD: "${GITSYNC_PASSWORD}"
      UID: 568
      GID: 568
    envFrom:
      - secretRef:
          name: home-assistant-secret

    controller:
      type: statefulset
      annotations:
        reloader.stakater.com/auto: "true"

    args:
      - "-depth=1"
      - "-v=3"

    persistence:
      - name: hass-config-v1
        mountPath: /hass-config
        existingclaim: hass-config-v1
    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false
    service:
      main:
        ports:
          http:
            port: 8080
