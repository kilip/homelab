---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  timeout: 600s
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    initContainers:
      01-init-db:
        image: ghcr.io/onedr0p/postgres-initdb:14.7
        imagePullPolicy: IfNotPresent
        envFrom:
          - secretRef:
              name: home-assistant-secret
        env:
          POSTGRES_SUPER_PASS: "${POSTGRES_ADMIN_PASS}"
      02-init-config:
        image: registry.k8s.io/git-sync/git-sync:v3.6.5
        envFrom:
          - secretRef:
              name: home-assistant-secret
        volumeMounts:
          - name: git-secret
            mountPath: /etc/git-secret/ssh/id_rsa
          - name: git-known-hosts
            mountPath: /etc/git-secret/known_hosts
        volumeClaimTemplates:
          - name: &config-pvc config-v1
            mountPath: /config
            accessMode: ReadWriteOnce
            size: 1Gi
            storageClass: ceph-block
    controller:
      type: statefulset
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: ghcr.io/kilip/home-assistant
      tag: 2023.4.6-c1
    env:
      TZ: "${TIMEZONE}"
      POSTGRES_HOST: postgresql.database.svc.cluster.local
      POSTGRES_DB: home_assistant
      POSTGRES_SUPER_PASS: "${POSTGRES_ADMIN_PASS}"
    envFrom:
      - secretRef:
          name: home-assistant-secret
    service:
      main:
        ports:
          http:
            port: 8123
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          hajimari.io/icon: mdi:home-assistant
        hosts:
          - host: &host "hass.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    persistence:
      ga-service-account:
        enabled: true
        type: secret
        name: home-assistant-secret
        defaultMode: 256
        subPath:
          - path: GA_SERVICE_ACCOUNT
            mountPath: /config/ga-service-account.json
      git-secret:
        enabled: true
        type: secret
        name: home-assistant-secret
        defaultMode: 256
        subPath:
          - path: GH_DEPLOY_KEY
            mountPath: /etc/git-secret/ssh/id_rsa
      git-known-hosts:
        enabled: true
        type: secret
        name: home-assistant-secret
        defaultMode: 256
        subPath:
          - path: GIT_SYNC_KNOWN_HOSTS
            mountPath: /etc/git-secret/known_hosts
    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false
    podSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
      fsGroupChangePolicy: OnRootMismatch
    volumeClaimTemplates:
      - name: *config-pvc
        mountPath: /config
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: ceph-block
    resources:
      requests:
        cpu: 10m
        memory: 100Mi
      limits:
        memory: 750Mi
