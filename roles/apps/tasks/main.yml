---
- name: Ensure librenms service configured
  ansible.builtin.include_tasks:
    file: librenms.yml
    apply:
      tags: librenms
  tags: librenms

- name: Ensure mosquitto tasks included
  ansible.builtin.include_tasks:
    file: mosquitto.yml
    apply:
      tags: mosquitto
  tags: mosquitto

- name: Ensure authentik tasks included
  ansible.builtin.include_tasks:
    file: authentik.yml
    apply:
      tags: authentik
  tags: authentik

- name: Ensure home assistant service configured
  ansible.builtin.include_tasks:
    file: hass.yml
    apply:
      tags: hass
  tags: hass

- name: Ensure trillium service configured
  ansible.builtin.include_tasks:
    file: trilium.yml
    apply:
      tags: trilium
  tags: trilium

- name: Ensure zabbix service configured
  ansible.builtin.include_tasks:
    file: zabbix.yml
    apply:
      tags: zabbix
  tags: zabbix
